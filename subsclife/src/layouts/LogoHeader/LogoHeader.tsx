import { Logo } from "@/assets/icons";
import * as Styled from "./LogoHeader.styled";

const LogoHeader = () => {
  return (
    <Styled.Container>
      <Logo />
    </Styled.Container>
  );
};

export default LogoHeader;
